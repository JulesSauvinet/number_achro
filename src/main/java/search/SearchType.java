package search;

/**
 * Cette classe est une enumeration de toutes les strategies possibles pour la resolution
 */
public enum SearchType {
    DEFAULT, GREEDY, ACTIVITY, MINDOM, INTVAR, MAXCONSTRAINTS,DOMOVERWDEG, RANDOM;
}
